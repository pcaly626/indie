"use client";

import styles from "./QuoteBox.module.css";
import { Box } from '../../ui/boxes/Box';

export const QuoteBox: any = () => {
    return (
        <Box pathname='/investigation' name="Investigation"/>
    )
}